-# app/views/api/v1/welcome/index.html.haml

-# This section adds a JavaScript snippet that enhances the page
-# by appending "ENHANCED!" to the first <h1> element and logging
- content_for :head do
  :javascript
    document.addEventListener("DOMContentLoaded", function() {
      document.getElementsByTagName('h1')[0].append(" 🐴");
      console.log(26666)
    })

%h1 #{pluralize(@page_hits, 'muul')} 